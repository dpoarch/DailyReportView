<h1>Create Product</h1>
<%= form_with model: @product, as: :product, url: create_product_path, html: { multipart: true, autocomplete: 'off' } do |form| %>
    <%= render partial: 'form', locals: {product: @product} %>
<% end %>

